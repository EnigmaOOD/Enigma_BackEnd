user nginx;
events {
 worker_connections 1000;
}

http {

   upstream localhost {
     server web:8000;
     }
       
   server {
    listen 8000;
    location / {
      proxy_pass http://localhost/;
     }
         }
}
